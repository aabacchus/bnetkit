#!/bin/sh -e

[ "$#" -lt 1 ] && {
    printf "usage: %s [gopher://]example.com/foo/bar\n" "$0" >&2
    exit 1
}


nopref="${1##gopher://}"
host="${nopref%%/*}"
path="${nopref#"$host"}"
path="${path:-/}"
printf "%s\r\n" "$path" | tee /dev/stderr | ./nc "$host" 70
